import styled from 'styled-components'

export const FlagElement = styled.div`
  align-items: center;
  display: flex;
  gap: 0.4rem;

  span {
    color: var(--text-grey-mid);
    font-size: 1.2rem;
    line-height: 1.8rem;
  }
`